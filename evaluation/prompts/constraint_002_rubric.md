# Constraint 002 Rubric — Offline Python / Standard Library Only

Maximum score: 10

1. Constraint compliance — 2 points
   - Uses only Python standard library.
   - Does not use or mention pip, requests, pandas, internet access, package installation, or third-party libraries.

2. Correct JSON loading — 2 points
   - Opens students.json correctly.
   - Parses JSON correctly.

3. Correct filtering logic — 2 points
   - Prints only names where score >= 50.
   - Does not print students below 50.

4. Missing-file handling — 1 point
   - Handles FileNotFoundError gracefully.

5. Malformed-JSON handling — 1 point
   - Handles json.JSONDecodeError gracefully.

6. Program completeness — 1 point
   - Code is complete and directly runnable.

7. Instruction following — 1 point
   - Gives only the complete program followed by a short explanation.

This prompt is frozen evaluation material and must not be used for training.
